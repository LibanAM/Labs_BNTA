/**
 * Classes here should be created according to some specification in the README file
 * There are some tests in the org.example.classes packeg (test dir).
 * After you create a class, run a test for it e.g., Account -> AccountTests.
 *
 * Account class is already created.
 */
package org.example.classes;